server:
  port: 8086
spring:
  application:
    name: ribbon-test
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka
    fetch-registry: true
    register-with-eureka: true
